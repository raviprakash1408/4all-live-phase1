import type { SpaceGridType } from './type';

export default {
  component: 'SpaceGrid',
  title: 'SpaceGrid',
  tags: ['SpaceGrid'],
};

export const Default: { args: SpaceGridType } = {
  args: {
    CameraName: 'Camera 01',
    id: 1,
  },
};
